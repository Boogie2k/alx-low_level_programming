#include <stdio.h>

/*
 * main - prints the numbers
 * numbsers are looped using the for loop
 * return: always return 0
 */

int main (void)
{
int i;
	for (i = 0; i<10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
